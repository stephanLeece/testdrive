{{> big-mac}} {{> side-menu}}

<div class="eventList" id="ddEventsList">
  <ul>
    {{#eventList}}
      <li class= {{eventClassName}}>{{eventTitle}}</li>
    {{/eventList}}
  </ul>
</div>

<div class="eventsInfoBox" id="ddEventsInfobox">
  {{#eventList}}
    <div class= {{eventClassName}}>
      {{#eventContent}}
      <img src={{image}} alt="">
        <p>{{imageDescrip}}</p>
      {{/eventContent}}
    </div>
  {{/eventList}}
  </div>
